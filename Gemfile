# frozen_string_literal: true
source "https://rubygems.org"

ruby '~> 2.3.0'

gem 'rack'

gem 'sprockets'

gem 'mini_racer' # the better JS for execjs used by uglifier
gem 'babel-transpiler' # es6
gem 'uglifier'

gem 'sass'
